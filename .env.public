# --- Non-Secret Configuration ---

# OpenAI Configuration (Models)
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Backend Configuration
LITEPAPER_SRC_DIR=https://github.com/ChaosChain/chaoschain-lite-paper.git
VECTOR_DB_DIR=./data/vector_store
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2
CHUNK_SIZE=1000
CHUNK_OVERLAP=150
SEARCH_K=4

# Frontend Configuration
VITE_API_URL=http://localhost:8000/api
# Expose source repo URL to frontend
NEXT_PUBLIC_LITEPAPER_SRC_URL=https://github.com/ChaosChain/chaoschain-lite-paper.git

# Add CORS Origins
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002 